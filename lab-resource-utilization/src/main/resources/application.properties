spring.application.name=lab-resource-utilization

spring.datasource.url=jdbc:postgresql://localhost:5432/mydb
spring.datasource.username=postgres
spring.datasource.password=dh@rshini03

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Enable data.sql seed file to run on startup
# spring.sql.init.mode=always

server.port=8086

# Email Configuration - Brevo (Free Email Service)
# Get your credentials from https://app.brevo.com/settings/keys/smtp
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=${BREVO_SMTP_LOGIN:priyadharshimanikandan25@gmail.com}
spring.mail.password=${BREVO_SMTP_PASSWORD:your-brevo-smtp-password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.debug=false

# Application settings
app.name=Lab Resource Platform
app.base-url=http://localhost:5173
app.otp.expiry-minutes=5
app.support-email=support@labresource.com
